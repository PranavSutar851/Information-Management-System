import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Venture Verse - Startup Idea Assessment Platform",
  description: "A comprehensive platform for startup idea assessment and funding decisions powered by AI",
  keywords: "startup, venture capital, AI assessment, funding, ideas",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
